// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gpgsmith/v1/vault.proto

package gpgsmithv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/excavador/locksmith/pkg/gen/gpgsmith/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// VaultServiceName is the fully-qualified name of the VaultService service.
	VaultServiceName = "gpgsmith.v1.VaultService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// VaultServiceListProcedure is the fully-qualified name of the VaultService's List RPC.
	VaultServiceListProcedure = "/gpgsmith.v1.VaultService/List"
	// VaultServiceStatusProcedure is the fully-qualified name of the VaultService's Status RPC.
	VaultServiceStatusProcedure = "/gpgsmith.v1.VaultService/Status"
	// VaultServiceOpenProcedure is the fully-qualified name of the VaultService's Open RPC.
	VaultServiceOpenProcedure = "/gpgsmith.v1.VaultService/Open"
	// VaultServiceResumeProcedure is the fully-qualified name of the VaultService's Resume RPC.
	VaultServiceResumeProcedure = "/gpgsmith.v1.VaultService/Resume"
	// VaultServiceSealProcedure is the fully-qualified name of the VaultService's Seal RPC.
	VaultServiceSealProcedure = "/gpgsmith.v1.VaultService/Seal"
	// VaultServiceDiscardProcedure is the fully-qualified name of the VaultService's Discard RPC.
	VaultServiceDiscardProcedure = "/gpgsmith.v1.VaultService/Discard"
	// VaultServiceSnapshotsProcedure is the fully-qualified name of the VaultService's Snapshots RPC.
	VaultServiceSnapshotsProcedure = "/gpgsmith.v1.VaultService/Snapshots"
	// VaultServiceImportProcedure is the fully-qualified name of the VaultService's Import RPC.
	VaultServiceImportProcedure = "/gpgsmith.v1.VaultService/Import"
	// VaultServiceExportProcedure is the fully-qualified name of the VaultService's Export RPC.
	VaultServiceExportProcedure = "/gpgsmith.v1.VaultService/Export"
	// VaultServiceTrustProcedure is the fully-qualified name of the VaultService's Trust RPC.
	VaultServiceTrustProcedure = "/gpgsmith.v1.VaultService/Trust"
)

// VaultServiceClient is a client for the gpgsmith.v1.VaultService service.
type VaultServiceClient interface {
	// List returns all configured vaults from the registry.
	List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error)
	// Status returns daemon-side state for vaults: which are currently open,
	// which have a recoverable ephemeral, etc.
	Status(context.Context, *connect.Request[v1.StatusVaultRequest]) (*connect.Response[v1.StatusVaultResponse], error)
	// Open asks the daemon to decrypt the latest canonical of the named
	// vault, perform TOFU on the embedded master fingerprint, and start an
	// in-memory Session. Returns the session info OR a ResumeAvailable
	// result if a recoverable ephemeral is on disk.
	Open(context.Context, *connect.Request[v1.OpenRequest]) (*connect.Response[v1.OpenResponse], error)
	// Resume continues a recoverable ephemeral session that Open just
	// discovered. The client passes the same vault name and indicates
	// resume vs discard.
	Resume(context.Context, *connect.Request[v1.ResumeRequest]) (*connect.Response[v1.ResumeResponse], error)
	// Seal writes the workdir as a new canonical snapshot, removes the
	// ephemeral file pair, and ends the session.
	Seal(context.Context, *connect.Request[v1.SealRequest]) (*connect.Response[v1.SealResponse], error)
	// Discard throws away the workdir and ephemeral files without sealing.
	Discard(context.Context, *connect.Request[v1.DiscardRequest]) (*connect.Response[v1.DiscardResponse], error)
	// Snapshots lists the canonical snapshots of a vault. Does NOT require
	// the vault to be open.
	Snapshots(context.Context, *connect.Request[v1.SnapshotsRequest]) (*connect.Response[v1.SnapshotsResponse], error)
	// Import takes an existing GNUPGHOME directory on disk and seals it as
	// the first snapshot of a new vault.
	Import(context.Context, *connect.Request[v1.ImportRequest]) (*connect.Response[v1.ImportResponse], error)
	// Export decrypts the latest canonical of the named vault to a target
	// directory on disk. No session, no lock, no daemon-side state. The
	// caller owns cleanup of the target directory. This is the escape hatch
	// for advanced/recovery use.
	Export(context.Context, *connect.Request[v1.ExportRequest]) (*connect.Response[v1.ExportResponse], error)
	// Trust replaces the trusted master fingerprint for a named vault. Used
	// after a legitimate master-key rotation when the user has confirmed the
	// new fingerprint out-of-band.
	Trust(context.Context, *connect.Request[v1.TrustRequest]) (*connect.Response[v1.TrustResponse], error)
}

// NewVaultServiceClient constructs a client for the gpgsmith.v1.VaultService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewVaultServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) VaultServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	vaultServiceMethods := v1.File_gpgsmith_v1_vault_proto.Services().ByName("VaultService").Methods()
	return &vaultServiceClient{
		list: connect.NewClient[v1.ListRequest, v1.ListResponse](
			httpClient,
			baseURL+VaultServiceListProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("List")),
			connect.WithClientOptions(opts...),
		),
		status: connect.NewClient[v1.StatusVaultRequest, v1.StatusVaultResponse](
			httpClient,
			baseURL+VaultServiceStatusProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Status")),
			connect.WithClientOptions(opts...),
		),
		open: connect.NewClient[v1.OpenRequest, v1.OpenResponse](
			httpClient,
			baseURL+VaultServiceOpenProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Open")),
			connect.WithClientOptions(opts...),
		),
		resume: connect.NewClient[v1.ResumeRequest, v1.ResumeResponse](
			httpClient,
			baseURL+VaultServiceResumeProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Resume")),
			connect.WithClientOptions(opts...),
		),
		seal: connect.NewClient[v1.SealRequest, v1.SealResponse](
			httpClient,
			baseURL+VaultServiceSealProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Seal")),
			connect.WithClientOptions(opts...),
		),
		discard: connect.NewClient[v1.DiscardRequest, v1.DiscardResponse](
			httpClient,
			baseURL+VaultServiceDiscardProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Discard")),
			connect.WithClientOptions(opts...),
		),
		snapshots: connect.NewClient[v1.SnapshotsRequest, v1.SnapshotsResponse](
			httpClient,
			baseURL+VaultServiceSnapshotsProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Snapshots")),
			connect.WithClientOptions(opts...),
		),
		_import: connect.NewClient[v1.ImportRequest, v1.ImportResponse](
			httpClient,
			baseURL+VaultServiceImportProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Import")),
			connect.WithClientOptions(opts...),
		),
		export: connect.NewClient[v1.ExportRequest, v1.ExportResponse](
			httpClient,
			baseURL+VaultServiceExportProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Export")),
			connect.WithClientOptions(opts...),
		),
		trust: connect.NewClient[v1.TrustRequest, v1.TrustResponse](
			httpClient,
			baseURL+VaultServiceTrustProcedure,
			connect.WithSchema(vaultServiceMethods.ByName("Trust")),
			connect.WithClientOptions(opts...),
		),
	}
}

// vaultServiceClient implements VaultServiceClient.
type vaultServiceClient struct {
	list      *connect.Client[v1.ListRequest, v1.ListResponse]
	status    *connect.Client[v1.StatusVaultRequest, v1.StatusVaultResponse]
	open      *connect.Client[v1.OpenRequest, v1.OpenResponse]
	resume    *connect.Client[v1.ResumeRequest, v1.ResumeResponse]
	seal      *connect.Client[v1.SealRequest, v1.SealResponse]
	discard   *connect.Client[v1.DiscardRequest, v1.DiscardResponse]
	snapshots *connect.Client[v1.SnapshotsRequest, v1.SnapshotsResponse]
	_import   *connect.Client[v1.ImportRequest, v1.ImportResponse]
	export    *connect.Client[v1.ExportRequest, v1.ExportResponse]
	trust     *connect.Client[v1.TrustRequest, v1.TrustResponse]
}

// List calls gpgsmith.v1.VaultService.List.
func (c *vaultServiceClient) List(ctx context.Context, req *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error) {
	return c.list.CallUnary(ctx, req)
}

// Status calls gpgsmith.v1.VaultService.Status.
func (c *vaultServiceClient) Status(ctx context.Context, req *connect.Request[v1.StatusVaultRequest]) (*connect.Response[v1.StatusVaultResponse], error) {
	return c.status.CallUnary(ctx, req)
}

// Open calls gpgsmith.v1.VaultService.Open.
func (c *vaultServiceClient) Open(ctx context.Context, req *connect.Request[v1.OpenRequest]) (*connect.Response[v1.OpenResponse], error) {
	return c.open.CallUnary(ctx, req)
}

// Resume calls gpgsmith.v1.VaultService.Resume.
func (c *vaultServiceClient) Resume(ctx context.Context, req *connect.Request[v1.ResumeRequest]) (*connect.Response[v1.ResumeResponse], error) {
	return c.resume.CallUnary(ctx, req)
}

// Seal calls gpgsmith.v1.VaultService.Seal.
func (c *vaultServiceClient) Seal(ctx context.Context, req *connect.Request[v1.SealRequest]) (*connect.Response[v1.SealResponse], error) {
	return c.seal.CallUnary(ctx, req)
}

// Discard calls gpgsmith.v1.VaultService.Discard.
func (c *vaultServiceClient) Discard(ctx context.Context, req *connect.Request[v1.DiscardRequest]) (*connect.Response[v1.DiscardResponse], error) {
	return c.discard.CallUnary(ctx, req)
}

// Snapshots calls gpgsmith.v1.VaultService.Snapshots.
func (c *vaultServiceClient) Snapshots(ctx context.Context, req *connect.Request[v1.SnapshotsRequest]) (*connect.Response[v1.SnapshotsResponse], error) {
	return c.snapshots.CallUnary(ctx, req)
}

// Import calls gpgsmith.v1.VaultService.Import.
func (c *vaultServiceClient) Import(ctx context.Context, req *connect.Request[v1.ImportRequest]) (*connect.Response[v1.ImportResponse], error) {
	return c._import.CallUnary(ctx, req)
}

// Export calls gpgsmith.v1.VaultService.Export.
func (c *vaultServiceClient) Export(ctx context.Context, req *connect.Request[v1.ExportRequest]) (*connect.Response[v1.ExportResponse], error) {
	return c.export.CallUnary(ctx, req)
}

// Trust calls gpgsmith.v1.VaultService.Trust.
func (c *vaultServiceClient) Trust(ctx context.Context, req *connect.Request[v1.TrustRequest]) (*connect.Response[v1.TrustResponse], error) {
	return c.trust.CallUnary(ctx, req)
}

// VaultServiceHandler is an implementation of the gpgsmith.v1.VaultService service.
type VaultServiceHandler interface {
	// List returns all configured vaults from the registry.
	List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error)
	// Status returns daemon-side state for vaults: which are currently open,
	// which have a recoverable ephemeral, etc.
	Status(context.Context, *connect.Request[v1.StatusVaultRequest]) (*connect.Response[v1.StatusVaultResponse], error)
	// Open asks the daemon to decrypt the latest canonical of the named
	// vault, perform TOFU on the embedded master fingerprint, and start an
	// in-memory Session. Returns the session info OR a ResumeAvailable
	// result if a recoverable ephemeral is on disk.
	Open(context.Context, *connect.Request[v1.OpenRequest]) (*connect.Response[v1.OpenResponse], error)
	// Resume continues a recoverable ephemeral session that Open just
	// discovered. The client passes the same vault name and indicates
	// resume vs discard.
	Resume(context.Context, *connect.Request[v1.ResumeRequest]) (*connect.Response[v1.ResumeResponse], error)
	// Seal writes the workdir as a new canonical snapshot, removes the
	// ephemeral file pair, and ends the session.
	Seal(context.Context, *connect.Request[v1.SealRequest]) (*connect.Response[v1.SealResponse], error)
	// Discard throws away the workdir and ephemeral files without sealing.
	Discard(context.Context, *connect.Request[v1.DiscardRequest]) (*connect.Response[v1.DiscardResponse], error)
	// Snapshots lists the canonical snapshots of a vault. Does NOT require
	// the vault to be open.
	Snapshots(context.Context, *connect.Request[v1.SnapshotsRequest]) (*connect.Response[v1.SnapshotsResponse], error)
	// Import takes an existing GNUPGHOME directory on disk and seals it as
	// the first snapshot of a new vault.
	Import(context.Context, *connect.Request[v1.ImportRequest]) (*connect.Response[v1.ImportResponse], error)
	// Export decrypts the latest canonical of the named vault to a target
	// directory on disk. No session, no lock, no daemon-side state. The
	// caller owns cleanup of the target directory. This is the escape hatch
	// for advanced/recovery use.
	Export(context.Context, *connect.Request[v1.ExportRequest]) (*connect.Response[v1.ExportResponse], error)
	// Trust replaces the trusted master fingerprint for a named vault. Used
	// after a legitimate master-key rotation when the user has confirmed the
	// new fingerprint out-of-band.
	Trust(context.Context, *connect.Request[v1.TrustRequest]) (*connect.Response[v1.TrustResponse], error)
}

// NewVaultServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewVaultServiceHandler(svc VaultServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	vaultServiceMethods := v1.File_gpgsmith_v1_vault_proto.Services().ByName("VaultService").Methods()
	vaultServiceListHandler := connect.NewUnaryHandler(
		VaultServiceListProcedure,
		svc.List,
		connect.WithSchema(vaultServiceMethods.ByName("List")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceStatusHandler := connect.NewUnaryHandler(
		VaultServiceStatusProcedure,
		svc.Status,
		connect.WithSchema(vaultServiceMethods.ByName("Status")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceOpenHandler := connect.NewUnaryHandler(
		VaultServiceOpenProcedure,
		svc.Open,
		connect.WithSchema(vaultServiceMethods.ByName("Open")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceResumeHandler := connect.NewUnaryHandler(
		VaultServiceResumeProcedure,
		svc.Resume,
		connect.WithSchema(vaultServiceMethods.ByName("Resume")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceSealHandler := connect.NewUnaryHandler(
		VaultServiceSealProcedure,
		svc.Seal,
		connect.WithSchema(vaultServiceMethods.ByName("Seal")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceDiscardHandler := connect.NewUnaryHandler(
		VaultServiceDiscardProcedure,
		svc.Discard,
		connect.WithSchema(vaultServiceMethods.ByName("Discard")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceSnapshotsHandler := connect.NewUnaryHandler(
		VaultServiceSnapshotsProcedure,
		svc.Snapshots,
		connect.WithSchema(vaultServiceMethods.ByName("Snapshots")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceImportHandler := connect.NewUnaryHandler(
		VaultServiceImportProcedure,
		svc.Import,
		connect.WithSchema(vaultServiceMethods.ByName("Import")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceExportHandler := connect.NewUnaryHandler(
		VaultServiceExportProcedure,
		svc.Export,
		connect.WithSchema(vaultServiceMethods.ByName("Export")),
		connect.WithHandlerOptions(opts...),
	)
	vaultServiceTrustHandler := connect.NewUnaryHandler(
		VaultServiceTrustProcedure,
		svc.Trust,
		connect.WithSchema(vaultServiceMethods.ByName("Trust")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gpgsmith.v1.VaultService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case VaultServiceListProcedure:
			vaultServiceListHandler.ServeHTTP(w, r)
		case VaultServiceStatusProcedure:
			vaultServiceStatusHandler.ServeHTTP(w, r)
		case VaultServiceOpenProcedure:
			vaultServiceOpenHandler.ServeHTTP(w, r)
		case VaultServiceResumeProcedure:
			vaultServiceResumeHandler.ServeHTTP(w, r)
		case VaultServiceSealProcedure:
			vaultServiceSealHandler.ServeHTTP(w, r)
		case VaultServiceDiscardProcedure:
			vaultServiceDiscardHandler.ServeHTTP(w, r)
		case VaultServiceSnapshotsProcedure:
			vaultServiceSnapshotsHandler.ServeHTTP(w, r)
		case VaultServiceImportProcedure:
			vaultServiceImportHandler.ServeHTTP(w, r)
		case VaultServiceExportProcedure:
			vaultServiceExportHandler.ServeHTTP(w, r)
		case VaultServiceTrustProcedure:
			vaultServiceTrustHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedVaultServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedVaultServiceHandler struct{}

func (UnimplementedVaultServiceHandler) List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.List is not implemented"))
}

func (UnimplementedVaultServiceHandler) Status(context.Context, *connect.Request[v1.StatusVaultRequest]) (*connect.Response[v1.StatusVaultResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Status is not implemented"))
}

func (UnimplementedVaultServiceHandler) Open(context.Context, *connect.Request[v1.OpenRequest]) (*connect.Response[v1.OpenResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Open is not implemented"))
}

func (UnimplementedVaultServiceHandler) Resume(context.Context, *connect.Request[v1.ResumeRequest]) (*connect.Response[v1.ResumeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Resume is not implemented"))
}

func (UnimplementedVaultServiceHandler) Seal(context.Context, *connect.Request[v1.SealRequest]) (*connect.Response[v1.SealResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Seal is not implemented"))
}

func (UnimplementedVaultServiceHandler) Discard(context.Context, *connect.Request[v1.DiscardRequest]) (*connect.Response[v1.DiscardResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Discard is not implemented"))
}

func (UnimplementedVaultServiceHandler) Snapshots(context.Context, *connect.Request[v1.SnapshotsRequest]) (*connect.Response[v1.SnapshotsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Snapshots is not implemented"))
}

func (UnimplementedVaultServiceHandler) Import(context.Context, *connect.Request[v1.ImportRequest]) (*connect.Response[v1.ImportResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Import is not implemented"))
}

func (UnimplementedVaultServiceHandler) Export(context.Context, *connect.Request[v1.ExportRequest]) (*connect.Response[v1.ExportResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Export is not implemented"))
}

func (UnimplementedVaultServiceHandler) Trust(context.Context, *connect.Request[v1.TrustRequest]) (*connect.Response[v1.TrustResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gpgsmith.v1.VaultService.Trust is not implemented"))
}
